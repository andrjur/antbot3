# 🚀 AntBot - Полная инструкция запуска

## ✅ Система готова к работе!

### 📋 Что установлено:
- ✅ **Docker инфраструктура** (бот + мониторинг)
- ✅ **Тестовая система** (pytest + coverage)
- ✅ **Prometheus** (метрики бота)
- ✅ **Grafana** (визуализация)
- ✅ **Alertmanager** (уведомления)

---

## 🏃‍♂️ Быстрый запуск (Windows CMD)

### Шаг 1: Запуск Docker
```cmd
# Откройте CMD и перейдите в папку
cd C:\Mentor\3ant

# Запуск всех сервисов
start.bat
```

### Шаг 2: Проверка статуса
```cmd
docker-compose ps
```

Должно быть:
```
NAME                  STATUS
antbot                Up (healthy)
antbot-grafana        Up
antbot-prometheus     Up
antbot-alertmanager   Up
```

---

## 🌐 Доступные сервисы

| Сервис | URL | Логин/Пароль | Статус |
|--------|-----|---------------|--------|
| **Бот** | http://localhost:8080 | - | ✅ |
| **Grafana** | http://localhost:3000 | admin/admin123 | ✅ |
| **Prometheus** | http://localhost:9090 | - | ✅ |
| **Alertmanager** | http://localhost:9093 | - | ⚠️ |

---

## 📊 Мониторинг в Grafana

### 1. Импорт дашборда:
1. Откройте http://localhost:3000
2. Логин: admin/admin123
3. Перейдите: Dashboards → Import
4. Загрузите файл: `monitoring/grafana/dashboards/antbot-dashboard.json`

### 2. Что мониторится:
- 📈 Активные пользователи (24ч, 7д, 30д)
- 📚 Отправленные уроки
- 📝 ДЗ на проверке
- 🚨 Ошибки бота
- ⏱️ Время работы
- 💡 Метрики N8N запросов

---

## 🧪 Тестирование

### Запуск тестов:
```cmd
pytest
```

### С покрытием:
```cmd
pytest --cov=. --cov-report=html
# Затем откройте htmlcov/index.html
```

---

## 🔧 Конфигурация

### Файлы:
- **.env** - основные настройки
- **settings.json** - курсы и тарифы
- **docker-compose.yml** - Docker сервисы

### Важные переменные в .env:
```
BOT_TOKEN=7473862113:AAFChEuDcqHA19dQPfbrO8L5MHOvt6HPi2M
ADMIN_GROUP_ID=-1002591981307
WEBHOOK_HOST=https://your-domain.com  # для production
```

---

## 📝 Команды управления

```cmd
# Перезапуск бота
docker-compose restart bot

# Просмотр логов бота
docker-compose logs -f bot

# Перезапуск всех сервисов
docker-compose restart

# Полная остановка
docker-compose down

# Сборка и запуск
docker-compose up --build -d
```

---

## 🚨 Устранение проблем

### Если бот не запускается:
```cmd
# Проверьте логи
docker-compose logs bot

# Проверьте .env файл (должен существовать)
dir .env

# Проверьте Docker Desktop запущен
docker version
```

### Если Grafana пустая:
1. Импортируйте дашборд из `monitoring/grafana/dashboards/`
2. Убедитесь что Prometheus показывает метрики бота

### Если Prometheus не видит метрики:
```cmd
curl http://localhost:8080/metrics
# Должен вернуть метрики бота
```

---

## 🎯 Что готово к разработке

### ✅ Тестирование:
- pytest с тестовой БД
- Coverage отчеты
- Unit тесты для всех модулей

### ✅ Мониторинг:
- Полные метрики бота
- Визуализация в Grafana
- Алерты в Telegram

### ✅ Инфраструктура:
- Docker контейнеры
- Volume-ы для данных
- Сеть для коммуникации

---

## 📞 Поддержка

При проблемах:
1. Проверьте `docker-compose logs bot`
2. Проверьте все сервисы: `docker-compose ps`
3. Перезапустите: `docker-compose restart`

**Система готова к разработке!** 🎉