# AntBot - Telegram-бот для образовательных курсов

## Описание

AntBot - это многофункциональный Telegram-бот, разработанный для автоматизации и управления образовательными курсами. Он позволяет пользователям активировать курсы, получать уроки по расписанию, отправлять домашние задания на проверку (в том числе с помощью AI) и взаимодействовать со службой поддержки.

---

## 🗄️ Структура Базы Данных (SQLite)

Бот использует базу данных SQLite для хранения всей информации. Ниже приведено подробное описание каждой таблицы.

### `users`
Хранит основную информацию о пользователях, которые взаимодействовали с ботом.
- `user_id` (INTEGER, PRIMARY KEY): Уникальный ID пользователя в Telegram.
- `username` (TEXT): Имя пользователя (@username).
- `first_name` (TEXT): Имя пользователя.
- `last_name` (TEXT): Фамилия пользователя.
- `timezone` (TEXT): Часовой пояс пользователя (по умолчанию 'Europe/Moscow').
- `registered_at` (TIMESTAMP): Дата и время первой регистрации.

### `courses`
Содержит определения курсов.
- `course_id` (TEXT, PRIMARY KEY): Строковый идентификатор курса (например, "база").
- `id` (INTEGER): Числовой, автоинкрементный ID.
- `group_id` (TEXT): ID Telegram-канала, откуда парсится контент для `LESSON_BASED` курсов.
- `title` (TEXT): Полное название курса.
- `course_type` (TEXT): Тип курса. `LESSON_BASED` (старая логика, уроки из канала) или `TASK_BASED` (новая логика, задания из `task_pool`).
- `message_interval` (REAL): Интервал между уроками в часах (по умолчанию 24).
- `description` (TEXT): Краткое описание курса.

### `course_versions`
Тарифы или версии курсов.
- `course_id` (TEXT): Внешний ключ к `courses.course_id`.
- `version_id` (TEXT): Идентификатор тарифа (например, "v1", "v2").
- `title` (TEXT): Название тарифа (например, "Соло", "С проверкой").
- `price` (REAL): Цена тарифа.
- `description` (TEXT): Описание тарифа.

### `user_courses`
Связующая таблица, отражающая прогресс пользователя по курсу. Это одна из самых важных таблиц.
- `user_id` (INTEGER): Внешний ключ к `users.user_id`.
- `course_id` (TEXT): Внешний ключ к `courses.course_id`.
- `version_id` (TEXT): Активный тариф пользователя.
- `status` (TEXT): Статус прохождения ('active', 'inactive', 'completed').
- `hw_status` (TEXT): Статус текущего домашнего задания ('none', 'pending', 'approved', 'rejected').
- `hw_type` (TEXT): Тип домашнего задания (например, 'text', 'photo').
- `current_lesson` (INTEGER): Номер последнего *отправленного* урока.
- `level` (INTEGER): Уровень сложности (для многоуровневых курсов).
- `first_lesson_sent_time` (DATETIME): Точное время отправки первого урока, используется как точка отсчета для расписания.
- `last_lesson_sent_time` (DATETIME): Время отправки последнего урока.
- `is_completed` (INTEGER): Флаг полного завершения курса (0 или 1).
- `last_menu_message_id` (INTEGER): ID последнего сообщения с главным меню для его обновления.
- `activation_date` (TIMESTAMP): Дата активации курса.

### `group_messages`
Содержит распарсенный контент уроков из каналов.
- `group_id` (TEXT): ID канала-источника.
- `lesson_num` (INTEGER): Номер урока, к которому относится контент.
- `course_id` (TEXT): ID курса.
- `content_type` (TEXT): Тип контента ('text', 'photo', 'video', etc.).
- `is_homework` (BOOLEAN): Является ли эта часть урока домашним заданием.
- `hw_type` (TEXT): Тип ожидаемого ДЗ.
- `text` (TEXT): Текстовое содержимое или подпись к медиа.
- `file_id` (TEXT): `file_id` для медиафайлов.
- `message_id` (INTEGER): ID исходного сообщения в канале.

### `course_activation_codes`
Кодовые слова для активации курсов.
- `code_word` (TEXT, PRIMARY KEY): Уникальный код активации.
- `course_id` (TEXT): Какой курс активируется.
- `version_id` (TEXT): Какой тариф активируется.
- `price_rub` (INTEGER): Цена, связанная с кодом.

### `pending_admin_homework`
Очередь домашних заданий, ожидающих проверки в админ-группе.
- `admin_message_id` (INTEGER, PRIMARY KEY): ID сообщения с ДЗ в админ-группе.
- `student_user_id` (INTEGER): ID студента.
- `course_numeric_id` (INTEGER): Числовой ID курса.
- `lesson_num` (INTEGER): Номер урока.
- `student_message_id` (INTEGER): ID исходного сообщения от студента.

### `user_actions_log`
Журнал всех значимых действий пользователя для аналитики и отладки.
- `user_id` (INTEGER): ID пользователя.
- `action_type` (TEXT): Тип действия ('COURSE_ACTIVATION', 'LESSON_SENT', 'HOMEWORK_SUBMITTED', etc.).
- `course_id`, `lesson_num`, `old_value`, `new_value`, `details`: Контекст действия.

### Таблицы для марафонов и геймификации (Новая логика)

- **`task_pool`**: Банк заданий для `TASK_BASED` курсов.
- **`marathon_flow`**: Определяет последовательность шагов (задания, инфо-сообщения, опросы) в `TASK_BASED` курсах.
- **`user_completed_tasks`**: Журнал выполненных пользователем заданий.
- **`medals`**: Определения всех возможных медалей и условий их получения (`trigger_type`, `trigger_details`).
- **`user_medals`**: Связующая таблица для хранения медалей, полученных пользователями.

---

## 🔬 Описание ключевых функций (`main.py`)

### Настройка и инициализация

- **`setup_logging()`**: Настраивает логирование в файл `bot.log` и в консоль с ротацией и локальным временем.
- **`load_settings()`**: Загружает `settings.json` и запускает `populate_course_versions` для наполнения тарифов в БД.
- **`init_db()`**: Создает все таблицы в `bot.db`, если они не существуют. **Важно:** при добавлении новых таблиц, их создание нужно прописать здесь.
- **`main()`**: Главная точка входа. Инициализирует все компоненты, настраивает веб-сервер для вебхуков и запускает бота.

### Работа с курсами и пользователями

- **`activate_course(...)`**: Активирует курс по кодовому слову. Обрабатывает случаи повторной активации и смены тарифа. Логирует действие и уведомляет админов.
- **`send_lesson_to_user(...)`**: Отправляет контент урока пользователю. Состоит из нескольких шагов:
    - `_get_lesson_content_from_db()`: Получает части урока из БД.
    - `_send_lesson_parts()`: Последовательно отправляет каждую часть (текст, фото, видео), проверяя `level` пользователя.
    - `_update_user_course_after_lesson()`: Обновляет `current_lesson` и `hw_status` в `user_courses`.
    - `_handle_course_completion()`: Если урок был последним, отправляет поздравительное сообщение и меняет статус курса на `completed`.
- **`check_lesson_schedule(...)`**: Фоновая задача, которая проверяет, пора ли отправлять следующий урок. Если `hw_status` не позволяет, проверка откладывается.
- **`get_next_lesson_time(...)`**: Рассчитывает и форматирует время до следующего урока в часовом поясе пользователя.

### Обработчики команд и состояний (FSM)

- **`start(...)`**: `/start`. Регистрирует пользователя и отправляет главное меню.
- **`show_my_courses(...)`**: `/mycourses`. Показывает список курсов пользователя.
- **`activate_command(...)` и `process_activation_code(...)`**: `/activate`. Управляют диалогом получения и обработки кода активации.
- **`cmd_support_callback(...)` и `process_support_message(...)`**: Управляют диалогом с техподдержкой, пересылая сообщения в админ-группу.

### Обработка домашних заданий

- **`process_homework(...)`**: Принимает ДЗ от пользователя, пересылает его в админ-группу и отправляет вебхук в n8n для AI-проверки.
- **`handle_homework_result(...)`**: Центральная функция для завершения проверки ДЗ. Уведомляет студента, обновляет статус в БД и запускает отправку следующего урока.
- **`handle_n8n_hw_approval(...)`**: Вебхук, принимающий результат проверки от n8n. Вызывает `handle_homework_result` или отправляет админу подсказку от AI, если ДЗ уже заблокировано.

### Администрирование

- **`admin_panel(...)`**: `/admin`. Открывает админ-панель.
- **`process_new_content(...)`**: Обработчик сообщений из каналов с контентом. Парсит теги и сохраняет уроки в `group_messages`.
- **`log_action(...)`**: Вспомогательная функция для записи всех значимых действий в `user_actions_log`.

---

## 📊 Диаграммы рабочих процессов

(Диаграммы остаются без изменений)

---

## 🧐 Архитектурные заметки и точки роста

(Раздел остается без изменений)

---

## 📜 Лицензия

Проект распространяется под лицензией MIT.